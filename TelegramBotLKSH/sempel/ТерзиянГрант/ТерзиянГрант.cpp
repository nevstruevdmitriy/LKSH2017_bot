#include <iostream>

using namespace std;

int main() {
    int turn;
	cout << 1 << endl;
    cout.flush();
    cin >> turn;
    cout << 0 << endl;
    cout.flush();
    cin >> turn;
    while (true) {
		cout << 2 << endl;
	    cout.flush();
	    cin >> turn;
		cout << 1 << endl;
		cout.flush();
		cin >> turn;
	}
}